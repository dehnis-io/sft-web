apiVersion: v1
kind: Service
metadata:
  name: {{ include "sft-web-helm-chart.fullname" . }}
  labels:
    {{- include "sft-web-helm-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sft-web-helm-chart.selectorLabels" . | nindent 4 }}
